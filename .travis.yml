dist: xenial
language: python

python:
  - "3.6"

install:
  - pip install -r requirements.txt
  - python setup.py install

script:
  - flake8 --show-source tokenizer
  - pytest

notifications:
  email: false
